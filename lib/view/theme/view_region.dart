import 'package:flutter/cupertino.dart';

class ViewRegion {
  // 圆角
  static double radiusSize = 24.0;
  static double smallRadiusSize = 12.0;

  // body 内边距
  static double scaffoldBodyPadding = 12.0;

  // 对话框外边距离
  static double dialogInsetPadding = 16.0;

  // 对话框标题
  static double dialogTitleFontSize = 20.0;
  static double dialogPadding = 24.0;

  // 列表标题大小
  static double tileTitleFontSize = 16.0;

  // 通用按钮大小
  static double buttonTextFontSize = 16.0;
  static double copyrightTextFontSize = 11.0;

  // 列表二级标题大小
  static double tileSubtitleFontSize = 14.0;
  static double panelFontSize = 14.0;

  // 列表字体间距
  static double tileTitleGap = 16.0;

  // 列表尾部间距
  static double tileTrailingGap = 8.0;

  // 列表间距离
  static double tileGap = 16.0;

  // 列表图标大小
  static double tileIconSize = 20.0;
  static double iconSize = 24.0;

  static SizedBox gap = SizedBox(height: 16);
  static SizedBox smallGap = SizedBox(height: 8);
  static SizedBox horizontalGap = SizedBox(width: 16);
  static SizedBox scaffoldHorizontalGap = SizedBox(width: scaffoldBodyPadding);
}
